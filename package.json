{
  "name": "google-ads-automation-suite",
  "version": "1.0.0",
  "private": true,
  "description": "Merged Google Ads automation suite — account provisioning, multi-MCC warm-up/farming, campaign monitoring and Telegram/email alerting behind a single login.",
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix backend start",
    "seed:admin": "npm --prefix backend run seed:admin",
    "seed:rules": "npm --prefix backend run seed:rules"
  },
  "engines": {
    "node": ">=18"
  }
}
